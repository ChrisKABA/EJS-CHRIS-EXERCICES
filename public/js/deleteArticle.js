const btnDelete = document.querySelector('.btnDelete');


function deleteArticle(params) {
    const articleTitle = document.querySelector('h1')
    if (articleTitle.textContent === article.title) {
        
    }

}

btnDelete.addEventListener('click',)